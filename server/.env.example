# Configuration du Serveur
PORT=3001
NODE_ENV=development

# Clé API Roblox
# Créez votre clé sur : https://create.roblox.com/dashboard/credentials
# ⚠️ PERMISSIONS MINIMALES REQUISES (READ-ONLY) :
#    ✅ universe:read - Lire les infos des jeux
#    ✅ economy:read - Lire les données économiques
#    ✅ transactions:read - Lire les transactions
#    ✅ asset:read - Lire les game passes et produits
# ❌ N'activez JAMAIS de permissions write, delete ou manage
ROBLOX_API_KEY=your_roblox_api_key_here

# IDs de vos jeux Roblox (séparés par des virgules)
# Trouvez vos Universe IDs dans l'URL de vos jeux ou via l'API Roblox
# Exemple : UNIVERSE_IDS=1234567890,9876543210
UNIVERSE_IDS=your_universe_ids_comma_separated

# Configuration du Cache (optionnel)
# Durée de vie du cache en secondes (par défaut: 300 = 5 minutes)
CACHE_TTL=300

# JWT Secret pour l'authentification multi-utilisateurs
# Générez avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# 🔐 Clé de chiffrement pour les cookies Roblox (AES-256)
# IMPORTANT: Générez une clé unique en production avec:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_ENCRYPTION_KEY=your_64_character_hex_encryption_key_here

# 🔔 Monitoring Automatique des Cookies de Session
# Activer/désactiver le monitoring automatique (default: true)
# Le système vérifie automatiquement la validité des cookies toutes les heures
# et envoie des notifications quand un cookie expire
ENABLE_COOKIE_MONITORING=true

# Notifications - Cookie Expiré (optionnels, mais recommandés)
# Email de l'administrateur
ADMIN_EMAIL=admin@example.com

# Discord Webhook URL
# Créez un webhook: Server Settings → Integrations → Webhooks → New Webhook
# Copiez l'URL du webhook et collez-la ici
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Slack Webhook URL
# Créez un webhook: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
