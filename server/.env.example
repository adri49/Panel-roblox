# Clé de chiffrement pour les cookies (AES-256-GCM)
# Générez une clé avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_ENCRYPTION_KEY=your_random_32_byte_hex_key_here

# Port du serveur (par défaut: 3001)
PORT=3001
